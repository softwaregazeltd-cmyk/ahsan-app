// Temporary role switch. Later this comes from Supabase auth/session.
// Change this value to preview each area: "public" | "client" | "admin"
export const ROLE: "public" | "client" | "admin" = "public";